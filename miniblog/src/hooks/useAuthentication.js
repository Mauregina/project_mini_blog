export const useAuthentication = () => {
    const createUser = 'Usuário criado';
    const error = 'deu erro';
    const loading = 'carregando';

    return {
        createUser,
        error,
        loading,
    };
};